---
name: create-slot
description: Build and maintain plugin-style React UI with create-slot — slots, hosts, fills and the plugin registry. Use when implementing, reviewing, debugging, or explaining code that uses create-slot.
---

# create-slot

Before working with create-slot, read the current [create-slot documentation](https://r13v.github.io/create-slot/llms.txt) and treat it as the source of truth.
